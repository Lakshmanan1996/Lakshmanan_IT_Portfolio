apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: portfolio
data:
  PORT: "5000"
  # Tighten this to your real frontend origin once you know it
  # (e.g. https://portfolio.yourdomain.com), instead of "*"
  ALLOWED_ORIGIN: "*"
